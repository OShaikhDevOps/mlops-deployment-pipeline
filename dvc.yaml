stages:
  train:
    cmd: python src/train.py
    deps:
      - src/train.py
    outs:
      - models/model.pkl

# Note: Configure DVC remote separately (dvc remote add) and track large datasets via dvc add <data>.
